name: Load Test

on:
 push:
  branches: [ "main", "dev" ]

jobs:
 artillery:
 
  runs-on: self-hosted

  steps:
  - name: Checkout
    uses: actions/checkout@v3
  - name: Run load test
    #uses: artilleryio/action-cli@v1.1.1
    #with:
    # command: run LoadTests/user_post.yml
    run: |
     artillery run load-test.yml
