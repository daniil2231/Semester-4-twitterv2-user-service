name: Run JMeter Tests

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - name: Run JMeter Tests
        run: |
          cd /Desktop/apache-jmeter-5.6.3/bin
          ./jmeter -n -t TwitterV2.jmx -l results.jtl
